const os = require("os");

// console.log(os.arch());
// console.log(os.freemem());

// const gb = os.freemem();
// console.log(`${gb / 1024 / 1024 / 1024}`);

// const freeTotalMem = os.totalmem();
// console.log(`${freeTotalMem / 1024 / 1024 / 1024}`);

// console.log(os.hostname());
// console.log(os.platform());
// console.log(os.type());
console.log(os.tmpdir());
